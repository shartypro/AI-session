# AI-session
AI project NOT FULLY AI JUST PROJECT this AI page is just for learning and not ready YET to use , soon it will be ready

# Requirments:
1 Python 3.11
2 pip install opencv-python 
3 python -m pip install mediapipe==0.10.9
4 python -c "import mediapipe as mp; print(hasattr(mp, 'solutions')); print(mp.solutions)"
